// Place third-party libraries here so that they can be split into
// a separate bundle.
require('moment');
require('stats.js');
require('splash-screen');

const THREE = require('three');
require('three-orbit-controls')(THREE);
require('three-collada-loader');
